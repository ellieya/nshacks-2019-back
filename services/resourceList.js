// // Todo: Rough logic - 
// function takes FILENAME (like "./folder/audio.wav")
// and returns
// ARRAY OF STRINGS containing the MEANING

// meaning = array of strings of most meaningful words said by user

// when MEANING (array of strings) is delivered, look up all entries in DB 
// categorized with that meaning and deliver as JSON array to front end
// function resourceList(meaning) {
//     resources = meaning.forEach(function(element){
//         if (db.records.find( { element: { $exists: true } } )

//     });
//     return resources;
// }

// module.exports = { resourceList }



    // "client": "cd client && npm run start",
    // "install": "cd client && npm install",
    // "build": "cd client && npm run build",
    // "heroku-postbuild": "npm run build"